{
  "name": "escuelastop-whatsapp-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Agente de atención virtual por WhatsApp + dashboard de administración para la Escuela de Manejo STOP",
  "workspaces": [
    "packages/backend",
    "packages/dashboard"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace packages/backend",
    "dev:dashboard": "npm run dev --workspace packages/dashboard",
    "build": "npm run build --workspace packages/backend && npm run build --workspace packages/dashboard",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down"
  },
  "engines": {
    "node": ">=20"
  }
}
